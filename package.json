{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vuepress dev src",
    "build": "vuepress build src",
    "push": "gh-pages -d dist -b main",
    "deploy": "npm run build && npm run push"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "vuepress": "^1.7.1",
    "vuepress-theme-meteorlxy": "^1.9.0"
  }
}
